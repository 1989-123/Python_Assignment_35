// Change each company name to uppercase one by one and print them out

const tech_jiant = ['Facebook', 'Google', 'Microsoft', 'Apple', 'IBM', 'Oracle', 'Amazon'];

for(let i = 0; i < tech_jiant.length; i++) {
    console.log(tech_jiant[i].toUpperCase());
}
